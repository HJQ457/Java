字节流：
    InputStream:
        FileInputStream 普通字节输入流
        BufferedInputStream 高效字节输入流（字节缓冲输入流）

    OutputStream：
        FileOutputStream 普通字节输出流
        BufferedOutputStream 高效字节输出流（字节缓冲输出流）

字符流：
    Reader:
        FileReader 普通字符输入流
        BufferedReader 高效字符输入流（字符缓冲输入流）

    Writer：
        FileWriter 普通字符输出流
        BufferedWriter 高效字符输出流（字符缓冲输出流）